# Development Environment Configuration

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/music_management_dev
spring.datasource.username=music_admin
spring.datasource.password=dev_password

# JPA Configuration
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging
logging.level.com.musicmanagement=DEBUG

# File Upload
app.upload.dir=uploads/dev/music
app.thumbnail.dir=uploads/dev/thumbnails

